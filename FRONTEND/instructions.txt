# Installation
npm install

# Development
npm run dev

# Production Build
npm run build

# Note:
# The project uses Vite with React.
# Ensure .env is set up correctly with VITE_API_BASE_URL before running the app.
